## 2025-02-18 - Icon-only Buttons Accessibility
**Learning:** Icon-only buttons (Trash, Play/Pause, Close) were pervasive and lacked accessible names.
**Action:** Always check `ManualCardEntry`, `ScriptViewer`, and `Toast` components for icon-only buttons and ensure `aria-label` is present.
